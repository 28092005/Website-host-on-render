// This controller is no longer used - authentication is handled in main routes
// Keeping file for potential future API endpoints

const User = require('../models/User');
const bcrypt = require('bcryptjs');

// Future API endpoints can be added here
module.exports = {};
